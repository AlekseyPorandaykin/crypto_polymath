// Package v1 содержит контракт REST API первой версии.
//
// Контракт встроен в бинарник, чтобы сервер отдавал по /docs/api/openapi.yaml
// ровно ту спецификацию, из которой сгенерированы обработчики
// (internal/ui/api/v1/spec) и клиент (api/rest/client/v1). Читать файл с диска
// в рантайме нельзя: рабочий каталог у контейнера свой, и документация
// разъехалась бы с кодом при первой же пересборке образа.
package v1

import _ "embed"

// Specification — исходный текст OpenAPI-контракта как есть, без разбора и
// перезаписи: клиенты и генераторы кода ожидают байт-в-байт тот же документ,
// что лежит в репозитории.
//
//go:embed openapi.yaml
var Specification []byte
