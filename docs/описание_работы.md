# Описание работы
## EMA
depth > 1
interval > 0

## MA
depth > 1
interval > 0

## PriceChanges
depth > 1
interval > 0

## StochasticMainLine
depth > 1
interval > 0


## TrendIndicator
Для тренда нужно накопить данные для анализа
depth >= 10
interval > 0

## TypeCandleIndicator
Тип свечи можно определить только для текущей свечи
depth == 1
interval > 0

## VolatilityCandlePercentIndicator
Изменчивость свечи можно определить только для текущей свечи
depth == 1
interval > 0

## RatioCandleToEMAIndicator
Изменчивость свечи можно определить только для текущей свечи. Отношение свечи к индикатору ema
depth == 1
interval > 0
IndicatorDepth = (все , которые есть)
result_depth = 1

## RatioCandleToMAIndicator
Отношение свечи к индикатору ma
depth == 1
interval > 0
IndicatorDepth = (все , которые есть
result_depth = 1

## MACDSHistogramIndicator 
depth =1
interval > 0
IndicatorDepth = 26 (по умолчанию для долгой(сигнальный) линии) MACDSignalLineIndicator
result_depth = 1

## MACDMainLineIndicator
depth =26
interval > 0
IndicatorDepth = 26 (по умолчанию для долгой(сигнальный) линии). Короткую запросим в бд. Если есть длинная линия, то должна быть и короткая 
result_depth = 1

## MACDSignalLineIndicator
depth =9
interval > 0
IndicatorDepth = 1
result_depth = 9

## RSIIndicator
depth >= 10 (все из конфига)
interval > 0
IndicatorDepth = все доступные у ema
result_depth = (все из конфига)

## StochasticSignalLine
depth = 3
interval > 0
IndicatorDepth = 1
result_depth = 3

## TrendByEMAIndicator
depth >= 10
interval > 0
IndicatorDepth >= 10
result_depth= (все из конфига)

## TrendByMAIndicator
depth >= 10
interval > 0
IndicatorDepth =>= 10
result_depth =  (все из конфига)